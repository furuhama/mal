# SPEC — Clojure ライク Lisp サブセット仕様 (mal)

バージョン: 0.1 (ドラフト・レビュー中)

## 1. 目的

純粋関数型言語の強み——**不変性**・**永続データ構造**・**STM（ソフトウェアトランザクショナルメモリ）による並行性**——を、Clojure ライクな Lisp のサブセットを **Rust** で実装することで体得することを目的とする。

- 本仕様は「何を実装するか」を定める。実装の詳細は `docs/design.md` に記す。
- 完全な Clojure 互換は目指さない（第 9 節「非目標」参照）。

## 2. 用語

- **式 (form)**: リーダーが生成するデータ。評価の対象。
- **値 (value)**: 評価の結果。データの他、関数・atom・ref・future を含む。
- **環境 (environment)**: シンボル → 値の写像のレキシカルな連鎖。
- **identity / state**: Clojure の「同一性と状態」モデル。atom / ref は identity であり、その state は常に不変値。状態の変化は「不変値の入れ替え」として表現する（第 8 節）。

## 3. 値の分類

| 分類 | 表記例 | 自己評価 | 備考 |
|---|---|---|---|
| nil | `nil` | はい | 偽 |
| 真偽値 | `true` / `false` | はい | `false` は偽 |
| 整数 | `42`, `-7` | はい | i64 範囲 |
| 浮動小数 | `3.14`, `-0.5` | はい | f64 |
| 文字列 | `"hello"` | はい | エスケープ対応 |
| キーワード | `:foo` | はい | 名前を文字列として保持 |
| シンボル | `foo` | いいえ | 環境で解決 |
| リスト | `(a b c)` | いいえ | 評価: 関数適用または特殊形式 |
| ベクタ | `[1 2 3]` | はい（要素は評価） | 不変・永続 |
| マップ | `{:a 1 :b 2}` | はい（要素は評価） | 不変・永続 |
| セット | `#{1 2 3}` | はい（要素は評価） | 不変・永続 |
| 関数 | `(fn [x] x)` | — | 組み込み / ユーザー定義 |
| atom | `(atom 0)` | — | Phase 3 |
| ref | `(ref 0)` | — | Phase 3 |
| future | `(future ...)` | — | Phase 3 |

### 3.1 真偽値

`false` と `nil` のみ偽。`0`・`""`・`[]`・`{}` はすべて真。条件式はこの規則で判定する。

### 3.2 数値

- 整数と浮動小数は別型だが、比較・算術では数値として相互に扱う（`(= 1 1.0)` は真）。
- 算術で一方が浮動小数なら結果は浮動小数。
- `(quot 7 2)` → `3`、`(rem 7 2)` → `1`（整数除算はこれらを使う）。
- 整数の除算 `/` は: 整数同士で割り切れる場合 `Int`、割り切れない場合 `Float`、浮動小数が絡む場合は `Float`。

### 3.3 型の区別

キーワード・シンボル・文字列は互換性のない別型（Clojure ではキーワードと同名シンボルが等価になるが、サブセットでは**区別する**。仕様上の意図的な簡略化）。

## 4. リーダー (Reader)

- 入力: 文字列。トークナイズ → パース。
- 空白区切り。コメント `;` は行末まで。
- トークン規則:
  - `(` `)` `[` `]` `{` `}` — コレクションの区切り
  - `'x` → `(quote x)` の糖衣
  - `@x` → `(deref x)` の糖衣（Phase 3 で有効。deref 未実装時は未束縛エラー）
  - 数値: `-?[0-9]+` → Int、`-?[0-9]+\.[0-9]+` → Float。i64 範囲外はリーダーエラー
  - 文字列: `"..."`。エスケープは `\"` `\\` `\n` `\t`
  - キーワード: `:` で始まり空白・括弧・`;`・`"` を含まない連続（`::` は非対応）
  - シンボル: 上記以外の連続。`true` / `false` / `nil` は対応するリテラルに変換
- パースエラーはメッセージ（可能なら行・列）付きで報告する。

## 5. プリンター (Printer)

- 読み取り可能表現（`pr-str` 系）: `read(print(x)) == x` のラウンドトリップを保証する。文字列はエスケープ、キーワードは `:` 付き、コレクションは括弧付きで出力。
- 表示用表現（`str` 系）: 文字列はそのまま出力。

## 6. 評価

### 6.1 基本規則

1. リテラル（nil・真偽値・数値・文字列・キーワード）は自分自身を返す。
2. コレクション（ベクタ・マップ・セット）は要素を評価して新しいコレクションを返す。
3. シンボルは環境から解決する。未定義ならエラー `unbound symbol: x`。
4. リストは先頭を検査する:
   - 先頭が特殊形式ならその規則に従う（6.2）。
   - それ以外は関数適用: 全要素を評価し、先頭が関数なら残りを引数として適用する。関数でなければエラー。

### 6.2 特殊形式

| 形式 | 動作 |
|---|---|
| `(def sym expr)` | `expr` を評価し、現在の環境に `sym` を束縛する。`sym` 自身を返す。 |
| `(fn [params] body...)` | クロージャを生成。`[a b & rest]` の可変長対応。`body` は最後の式の値（暗黙の `do`）。単一 arity のみ。 |
| `(defn name [params] body...)` | `(def name (fn [params] body...))` の糖衣。 |
| `(let [b1 v1 b2 v2 ...] body...)` | 並行バインディング: 右辺はすべて**束縛前の環境**で評価し、まとめて束縛した環境で `body` を評価する。 |
| `(if c then else?)` | `c` が真なら `then`、偽なら `else`（省略時 `nil`）。選択されない側は評価しない。 |
| `(do form...)` | 順に評価し、最後の式の値を返す。0 個なら `nil`。 |
| `(quote form)` | `form` を未評価で返す。`'` 糖衣あり。 |
| `(and e1 e2 ...)` | 左から評価し、偽（`false`/`nil`）に出会ったらその値を返して停止。すべて真なら最後の値を返す（短絡評価）。 |
| `(or e1 e2 ...)` | 左から評価し、真に出会ったらその値を返して停止。すべて偽なら最後の値を返す（短絡評価）。 |
| `(cond c1 e1 c2 e2 ...)` | 上から条件を評価し、最初に真になった条件に対応する式を評価して返す。すべて偽なら `nil`。Clojure ではマクロだが、サブセットでは特殊形式として提供。 |
| `(when c body...)` | `(if c (do body...))` の糖衣。Clojure ではマクロだが、サブセットでは特殊形式として提供。 |
| `(loop [b1 v1 ...] body...)` | `let` と同様に束縛して `body` を評価する。`body` 内の `(recur e1 ...)` は**末尾位置のみ**で許可。 |
| `(recur e1 ...)` | `loop` の束縛を新しい値で置き換えて再実行（TCO・スタックを消費しない）。`fn` 本体の末尾位置では fn のパラメータを再束縛する（Clojure 準拠）。末尾位置以外ではエラー。 |
| `(dosync body...)` | Phase 3。トランザクションを開始して `body` を実行する。 |

### 6.3 コア関数

シグネチャは Clojure に準拠する。エッジケースの詳細は `docs/core-functions.md` に実装時に追記する。

- **算術**: `+` `-` `*` `/`（可変長）、`quot` `rem` `inc` `dec` `max` `min` `abs`
- **比較**: `=` `not=` `<` `>` `<=` `>=`
- **述語**: `nil?` `true?` `false?` `number?` `int?` `float?` `string?` `keyword?` `symbol?` `list?` `vector?` `map?` `set?` `fn?` `seq?` `empty?` `pos?` `neg?` `zero?` `even?` `odd?`
- **リスト / シーケンス**: `list` `cons` `first` `rest` `next` `conj` `seq` `count` `nth` `last`
- **ベクタ**: `vector` `vec` `nth` `conj`（末尾追加）
- **マップ**: `hash-map` `get` `assoc` `dissoc` `contains?` `keys` `vals` `merge`
- **セット**: `set` `conj` `disj` `contains?`
- **高階**: `map` `filter` `reduce` `apply` `partial` `comp` `identity` `constantly`
- **文字列 / 出力**: `str` `pr-str` `print` `println` `read-string`
- **変換**: `int` `float` `keyword` `symbol` `name`
- **STM（Phase 3）**: `atom` `deref` `swap!` `reset!` `ref` `ref-set` `alter` `commute` `ensure`
- **その他**: `time`（任意）

補足:
- マップのキー等価性は `=` に従う（`1` と `1.0` は同一キー）。
- `get` はキー不在で `nil`。`nth` は範囲外でエラー。
- 関数の等価性は同一性のみ（`=` は同一の関数オブジェクトでのみ真）。

### 6.4 エラー

- 評価エラーは値として返さず、エラーとして伝播する。
- 種別: 未束縛シンボル / 引数個数不一致 (arity) / 型エラー / 範囲外アクセス / STM 違反 / リーダーエラー。
- エラーメッセージは人間が読める形式にする。

## 7. 永続データ構造 (Phase 2) — 実装済み

- **不変性**: すべての更新操作は新しい値を返し、元の値は決して変わらない。
- **構造共有**: 更新コスト O(log n)。
  - ベクタ: 32-way 分岐の永続トライ + tail 最適化（Clojure の PersistentVector 相当）。追加・参照・更新 O(log₃₂ n)
  - マップ: **8 エントリ以下は挿入順を保つ Array、超えると HAMT**（hash array mapped trie）に昇格（Clojure の array-map → hash-map と同じ方式）
  - セット: マップの値なし版
  - リスト: cons セル（単方向連結リスト、先頭追加 O(1)）
- キーのハッシュは `=` と整合する（`1` と `1.0` は同一キー）。
- Phase 1 の単純実装からの置き換えは、**挙動が同一であることをゴールデンテストで保証した**（全テストパス）。
- ベンチマーク: `cargo run --release --example bench`（100 万要素: ベクタ get ≈ 18 ns/op、conj ≈ 536 ns/op、マップ get ≈ 487 ns/op）。

## 8. 並行性と STM (Phase 3) — 実装済み

### 8.1 identity / state モデル

値は不変。時間とともに変化するものは identity（atom / ref）が指す不変値の**入れ替え**として表現する。これは本プロジェクトの中核テーマであり、`dosync` の原子性・一貫性・分離性はこれにより保証される。

### 8.2 Atom（単一参照の原子的更新）

- `(atom x)` — 初期値 `x` の atom を生成。
- `(deref a)` / `@a` — 現在値を返す（一貫したスナップショット）。
- `(swap! a f & args)` — 現在値 `v` に `(f v & args...)` を適用して原子的に更新。新しい値を返す。関数は純粋であることを想定（複数回適用されうる）。
- `(reset! a x)` — 値を `x` に置き換える。

### 8.3 Ref と STM

- `(ref x)` — トランザクション内でのみ変更できる参照。
- `(dosync & body)` — トランザクション:
  1. `body` を評価する。`ref-set` / `alter` / `commute` による書き込みはステージされる（まだ確定しない）。
  2. 読み取りはトランザクション内一貫性のある値（read-set のキャッシュ。自分の書き込みは反映される）を返す。
  3. コミット時、read-set の各 ref のバージョンがトランザクション中に他スレッドから変更されていないか検証する。変更されていればトランザクション全体を再実行する（上限 10000 回。超過はエラー）。
  4. コミット成功で全書き込みが原子的に適用される（コミットはグローバルロックで直列化）。
- `(ref-set r v)` — 値を設定する。トランザクション外ではエラー。
- `(alter r f & args)` — 書き込みをステージし、コミット時に `(f 現在値 & args...)` を適用する。書き込みは read-set に含めないため、**他のトランザクションの書き込みとは競合しない**（並行する alter はすべてコミットされ、順に適用される）。読み取り（`deref`）と組み合わせることで検証が効く。
- `(commute r f & args)` — alter と同じくコミット時に最新値へ適用。順序に依存しない更新（カウンターなど）向け。本サブセットでは alter と同一の実装（概念的な用途の違いのみ）。
- `(ensure r)` — `r` を読み取り、read-set に記録する（他トランザクションの書き込みと競合させる）。
- トランザクションは**再実行されうる**ため、`dosync` 内の副作用（`println` や `atom` の更新）は複数回実行されうる。純粋なコードを書くこと。
- ネストした `dosync` は同じトランザクションに統合される。

### 8.4 Future（並行実行）

- `(future & body)` — 別スレッドで `body` を実行し、future を返す。
- `(deref f)` / `@f` — 結果を待って返す（ブロック）。future 内でエラーが起きた場合は `deref` 時にエラーとして再送出。

### 8.5 デモプログラム（受け入れ基準）

- **口座振込**: 複数口座（`ref`）間の振込を並行実行し、不変条件「総額不変・残高非負」が STM により常に保たれることを確認する。`demos/bank-transfer.mal` と `tests/stm.rs` を参照。

```clojure
(def acc-a (ref 1000))
(def acc-b (ref 1000))

(defn transfer [from to amount]
  (dosync
    (alter from - amount)
    (alter to + amount)))

;; 並行して transfer を実行しても、総額 2000 は不変
;; 残高が負になることはない（atom やロックなしの変数では保証が難しい）
```

- **並行カウンター**: `atom` を複数スレッドから `swap!` で更新し、最終値が正確であることを確認する（8 スレッド × 1000 回 = 8000）。
- Phase 1 の動作例:

```clojure
(defn fib [n]
  (loop [i 0 a 0 b 1]
    (if (= i n) a (recur (inc i) b (+ a b)))))
(println (fib 10))   ; => 55
```

## 9. 非目標（今回のスコープ外）

- Java・ホスト相互運用、`ns`・名前空間
- lazy seq / シーケンスの遅延評価
- protocol / multimethod / record / type
- 有理数・分数、文字型 (char)、正規表現リテラル
- スレッド生成の高級 API（`future` のみ提供）
- マクロの完全な再帰・ハイジーン（gensym）。マクロは**非ハイジーン**（シンボル捕捉の可能性あり）
- シンタックスクォートの名前空間修飾。quasiquote のシンボルはそのまま保持される

## 9.5 Phase 4（マクロ・メタデータ・例外）— 実装済み

### 9.5.1 マクロ (defmacro)

- `(defmacro name [params] body...)` — マクロを定義。呼び出し時に**未評価の引数**で展開し、展開結果を評価する。`defn` と同様にドキュメント文字列を付与できる。
- 展開はコード生成: `quasiquote`（`` ` ``）/ `unquote`（`~`）/ `unquote-splicing`（`~@`）の糖衣を提供する。
- `(macroexpand-1 form)` / `(macroexpand form)` — 展開結果の確認用。
- マクロは関数として適用できない（`:syntax` エラー）。
- **非ハイジーン**: マクロが生成するシンボルは呼び出し側の環境に干渉しうる。

### 9.5.2 メタデータ

- `(with-meta x m)` — 値にメタデータ（マップ）を付与。`(meta x)` — 取得（なければ `nil`）。
- メタデータは**等価性・表示・ハッシュ・関数適用で透過**（`(= (with-meta [1] m) [1])` は真）。
- `(defn name "doc" [params] body...)` — ドキュメント文字列は `:doc` メタデータになる。

### 9.5.3 try / catch / finally

- `(try body... (catch e body...)? (finally body...)?)` — エラーを捕捉。
- `e` には `{:message "... " :kind :unbound|:type|...}` のマップを束縛。
- `(throw x)` — ユーザーエラーを投げる（文字列または `:message` を持つマップ）。
- `finally` は成功・失敗にかかわらず実行される。
- キーワードは関数としてマップ検索に使える（`(:k m)` = `(get m :k)`）。

## 10. REPL / 実行

- `mal` — REPL 起動。プロンプト `mal=> `。評価エラーは表示して継続。Ctrl-D または `(exit)` で終了。
- `mal file.mal` — ファイルを順に評価する。`println` 等の出力のみを stdout に出す。エラー時は exit code 1。

## 11. テスト方針

- **ゴールデンテスト**: `tests/*.mal` と期待 stdout のペア（MAL プロジェクトの方式に倣う）。
- **Rust テスト**: リーダーのラウンドトリップ、評価結果、エラー系。
- **STM ストレステスト**: 複数スレッドで振込・カウンターを実行し、不変条件を検証する。

## 12. ロードマップ

| Phase | 内容 | 完了条件 |
|---|---|---|
| 1 | コア言語: reader / printer / eval / 特殊形式 / コア関数 / REPL | ゴールデンテストが通る |
| 2 | 永続データ構造の本実装（自作） | 挙動同一 + ベンチマーク |
| 3 | atom / ref / STM / future + デモ | 並行テストが通る |
| 4 | defmacro / quasiquote / metadata / try-catch | ゴールデンテストが通る |

## 13. 変更履歴

- 0.1: 初版ドラフト
